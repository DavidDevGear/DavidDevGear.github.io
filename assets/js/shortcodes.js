// Shortcodes JS - Minimal version since theme.js handles most
console.log('Shortcodes JS loaded');

// Simple initialization
document.addEventListener('DOMContentLoaded', function() {
    console.log('Shortcodes initialized');
    
    // Any shortcode-specific functionality can go here
    // Most functionality is in theme.js
});
